import { types } from "./type";
import { mutations } from "./mutations";
import { resolvers } from "./resolvers";

const queries = ``;

export const Comments = { types, mutations, resolvers, queries };
